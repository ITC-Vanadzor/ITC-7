# include <iostream>
#include <math.h>
using std::cin;
using std::cout;
using std::endl;

int main()
{
 int i,x;
 int n;
 for (n=1; n<=12 ; ++n) {
	if (sqrt(n*12)==int (sqrt(n*12)) )
          {
             cout<<"\n n= "<<n;
             break;
	  }	
}


return 0;
}
